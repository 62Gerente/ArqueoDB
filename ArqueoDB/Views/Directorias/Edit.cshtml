@model ArqueoDB.Models.Directoria

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Directoria</legend>

        @Html.HiddenFor(model => model.DirectoriaID)

        <div class="editor-label">
            @Html.LabelFor(model => model.Caminho)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Caminho)
            @Html.ValidationMessageFor(model => model.Caminho)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
