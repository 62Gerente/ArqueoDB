@model IEnumerable<ArqueoDB.Models.Directoria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Caminho)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Caminho)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DirectoriaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DirectoriaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DirectoriaID })
        </td>
    </tr>
}

</table>
