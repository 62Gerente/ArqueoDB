@model IEnumerable<ArqueoDB.Models.ImagemLocal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Imagem.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Local.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Imagem.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Local.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ImagemLocalID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ImagemLocalID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ImagemLocalID })
        </td>
    </tr>
}

</table>
