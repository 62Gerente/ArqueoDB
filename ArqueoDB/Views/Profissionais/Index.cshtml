@model IEnumerable<ArqueoDB.Models.Profissional>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Utilizador.NomeUtilizador)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Utilizador.NomeUtilizador)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProfissionalID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProfissionalID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProfissionalID })
        </td>
    </tr>
}

</table>
