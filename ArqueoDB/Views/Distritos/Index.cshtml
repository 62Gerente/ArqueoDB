@model IEnumerable<ArqueoDB.Models.Distrito>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pais.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pais.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DistritoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DistritoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DistritoID })
        </td>
    </tr>
}

</table>
